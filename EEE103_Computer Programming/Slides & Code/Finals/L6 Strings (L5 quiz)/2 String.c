#include <stdio.h>

int main(){


char dept[] = {'E', 'E', 'E'}; //Must be char arr[]. Null character must for this type of declaration
char dept_name[] = "EEE";
printf("Str: %s\n", dept_name);

printf("Null Op: %s", dept);






return 0;}
